# pip install -r requirements-dev.txt
# pre-commit install
# pre-commit run -a

pre-commit  # Virtual environment'da çalıştır
cryptography>=40.0.0 # "Self Signed" sertifika oluşturmak için: python scripts/generate_cert.py

pip install jsbeautifier # .js dosyalarını biçimlendirmek için
# Örnek kullanım (Proje kökünde): js-beautify -o app/static/js/file-name.js app/static/js/file-name.js
